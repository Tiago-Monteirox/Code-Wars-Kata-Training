def move_zeros(lst):
    return [n for n in lst if n != 0] + [zero for zero in lst if zero == 0]


print(move_zeros([1, 0, 0, 0, 4, 6, 0, 2, 3]))

# def move_zeros(array):
#     for i in array:
#         if i == 0:
#             array.remove(i) # Remove the element from the array
#             array.append(i) # Append the element to the end
#     return array