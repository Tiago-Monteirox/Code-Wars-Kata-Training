def opposite(number):
    if number > 0:
        number = -abs(number)
        return number
    else:
        number = abs(number)
        return number

# def opposite(number):
#     return -number

# def opposite(number):
#   return number * -1